\documentclass[tikz,border=10pt]{standalone}
\input{../../../docs/tikz/dag_preamble.tex}

\begin{document}
\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  every node/.append style={outer sep=4pt, font=\sffamily\small}
]

\dagPaperMetadata{Test-optional Policies: Overcoming Strategic Behavior and Informational Gaps}{Zhi Liu and Nikhil Garg}{EAAMO 2021 / arXiv:2107.08922}{2021}{https://arxiv.org/abs/2107.08922}
\dagPaperLegendRightOfMetadata{
  \node[dag_result, dag_template_legend] (legClosed) at (0,0) {checked component\\or local result};
  \node[dag_conditional, dag_template_legend] (legConditional) at (4.2,0) {conditional or\\diagnostic component};
  \node[dag_unformalized, dag_template_legend] (legOpen) at (8.7,0) {open research\\direction};
}
\daglegend{(legClosed)(legConditional)(legOpen)}{Legend}

\begin{dagPaperBody}

\node[dag_model, text width=4.7cm] (Model) at (0,0) {
  \textbf{Gaussian source model}\\
  skills, features, access, requirements, and decisions
};

\node[dag_result, text width=4.8cm] (Defs) at (6.0,0) {
  \textbf{Definitions 1--6}\\
  equilibrium, fairness, test blankness, and resampling kernels
};

\node[dag_result, text width=5.0cm] (Thm31) at (12.2,0) {
  \textbf{Theorem 3.1}\\
  independent optional and report-required source-timed routes
};

\node[dag_conditional, text width=5.2cm] (Thm32Diagnostic) at (18.8,0) {
  \textbf{Theorem 3.2 scope comparison}\\
  arbitrary randomized policy is observably fair and nonblank in a checked weak equilibrium
};

\node[dag_result, text width=5.4cm] (Thm32) at (25.8,0) {
  \textbf{Approved Theorem 3.2 component}\\
  deterministic reported-score output; arbitrary base law; operational a.e. blankness
};

\node[dag_conditional, text width=5.2cm] (Clarifications) at (32.5,0) {
  \textbf{Source-model clarifications}\\
  mixture, continuity, observability, affine cutoff, and marginal precision
};

\node[dag_result, text width=5.1cm] (Lemma41) at (3.0,-5.2) {
  \textbf{Lemma 4.1}\\
  a.e. selected-action behavior under active-branch refinement
};

\node[dag_result, text width=5.0cm] (Props) at (9.5,-5.2) {
  \textbf{Propositions 4.2--4.3}\\
  independent actual-law fairness routes
};

\node[dag_result, text width=5.2cm] (Resampling) at (16.1,-5.2) {
  \textbf{Definition 6 and Theorem 4.4}\\
  source-derived resampling and independent protocol fairness routes
};

\node[dag_result, text width=5.0cm] (Transport) at (22.7,-5.2) {
  \textbf{Reporting-conditioned transport}\\
  selected law-level generalization from access to reporting
};

\node[dag_unformalized, text width=5.4cm] (Open) at (30.0,-5.2) {
  \textbf{Source-declared open directions}\\
  hidden-access demographic fairness, beyond Gaussian signals, approximate fairness
};

\node[dag_template_note, text width=31.5cm] (StatusNote) at (16.3,-9.7) {
  \textbf{Current review state: final closeout pending.}  The selected source
  claims are represented under the explicit source-timed active-branch
  refinement; Theorem 3.1's regimes are independent, and Section 4 uses
  actual-law routes without invented null-branch payoffs. The
  reporting-conditioned generalization, Theorem 3.2 scope comparison, and
  source-model clarifications are represented explicitly.
};

\draw[dag_arrow] (Model.east) -- (Defs.west);
\draw[dag_dashed_arrow] (Defs.east) -- (Thm31.west);
\draw[dag_dashed_arrow] (Thm31.east) -- (Thm32Diagnostic.west);
\draw[dag_dashed_arrow] (Thm32Diagnostic.east) -- (Thm32.west);
\draw[dag_dashed_arrow] (Thm32.east) -- (Clarifications.west);

\draw[dag_dashed_arrow] (Model.south) |- (Lemma41.west);
\draw[dag_dashed_arrow] (Lemma41.east) -- (Props.west);
\draw[dag_dashed_arrow] (Defs.south) |- (Resampling.west);
\draw[dag_dashed_arrow] (Props.east) -- (Resampling.west);
\draw[dag_dashed_arrow] (Thm32.south) |- (Transport.north);
\draw[dag_dashed_arrow] (Resampling.east) -- (Transport.west);
\draw[dag_dashed_arrow] (Transport.east) -- (Open.west);

\end{dagPaperBody}
\end{tikzpicture}
\end{document}
