\documentclass[tikz,border=10pt]{standalone}
\input{../../../docs/tikz/dag_preamble.tex}

\begin{document}
\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  every node/.append style={outer sep=4pt}
]

\dagPaperMetadata{Balancing Producer Fairness and Efficiency via Prior-Weighted Rating System Design}{Thomas Ma, Michael S. Bernstein, Ramesh Johari, and Nikhil Garg}{Proceedings of the International AAAI Conference on Web and Social Media 19(1)}{2025}{https://doi.org/10.1609/icwsm.v19i1.35865}

\dagPaperLegendRightOfMetadata{
\node[dag_model, dag_template_legend] (legDef) at (0,0) {source model\\or domain};
\node[dag_result, dag_template_legend] (legResult) at (3.7,0) {formalized\\named result};
\node[dag_template_note, dag_template_legend, text width=3.0cm] (legNote) at (7.4,0) {formalized\\source correction};
}
\daglegend{(legDef)(legResult)(legNote)}{Legend}

\begin{dagPaperBody}

\node[dag_model, text width=5.4cm] (FixedModel) at (0,0) {
  \textbf{Fixed binary-rating model} \\
  positive discrete review count; Beta shape and nonnegative strength; \\
  conditional posterior mean, bias, variance, and squared bias
};

\node[dag_model, text width=5.4cm] (Domains) at (0,-4.1) {
  \textbf{Source theorem domains} \\
  positive Beta-shape parameters; $q\in[0,1]$; \\
  nonnegative and ordered prior strength
};

\node[dag_result, text width=5.6cm] (Thm31) at (7.4,0) {
  \textbf{Theorem 3.1} \\
  squared bias nondecreasing; variance weakly decreasing on $[0,1]$ and strictly decreasing on $(0,1)$
};

\node[dag_result, text width=5.6cm] (Thm32) at (7.4,-4.1) {
  \textbf{Theorem 3.2} \\
  squared-bias convexity and prior-mean minimum; variance concavity and maximum at $q=1/2$
};

\node[dag_template_note, text width=5.2cm] (EndpointCorrection) at (14.6,0) {
  \textbf{Theorem 3.1 correction} \\
  The printed universal strict claim fails at $q=0,1$. The formalized target records weak closed-interval and strict interior conclusions.
};

\node[dag_template_note, text width=9.9cm] (AppendixCorrections) at (7.3,-8.0) {
  \textbf{Supplementary Appendix corrections} \\
  Equation 20 uses $\mathrm{Beta}(C,1-C)$, compatible with $(k+mC)/(n+m)$. Equation 21 includes Dirichlet prior pseudo-counts in both sums; its printed display is the zero-prior special case.
};

\draw[dag_arrow] (FixedModel) -- (Thm31);
\draw[dag_arrow] (Domains) -- (Thm31);
\draw[dag_arrow] (FixedModel) -- (Thm32);
\draw[dag_arrow] (Domains) -- (Thm32);
\draw[dag_dashed_arrow] (Thm31) -- (EndpointCorrection);
\draw[dag_dashed_arrow] (FixedModel.south east) to[out=-45,in=110] (AppendixCorrections.north west);

\end{dagPaperBody}
\end{tikzpicture}
\end{document}
